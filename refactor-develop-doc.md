Additional documentation for refactor-develop branch
====================================================

To do local compilation:

`./localcompile.py`

and additionally follow by `webpack` to bundle code into single JS source files for other modules to use
